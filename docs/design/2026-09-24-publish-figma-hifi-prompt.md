请根据我提供的截图，在当前 Figma 文件中还原一个「校园资源小程序—发布资源」的可编辑高保真原型，用于后续调整视觉细节及开发对照。以截图为视觉依据，忠实还原配色、字体层级、布局、圆角、边框、按钮和图标，不重新设计页面。

保真度要求：高保真。文字使用真实可编辑文本，界面控件使用独立的 Figma 图层。不要交付灰度线框，也不要将整张截图贴进 Frame 作为最终结果。

1. 画布与组件复用

新建页面 Frame，命名「发布资源 / 高保真 / 默认态」，尺寸约 390 × 844 px。原截图为 853 × 1844 px，以下位置与尺寸均为换算后的近似值；出现微小偏差时，优先匹配截图的比例、对齐和视觉效果。

底部导航复用要求：用户已确认当前页面已有底部导航栏组件，本页必须复用。具体节点与可用变体尚未核实，请先定位当前页面的现有导航实例及其源组件，再按第 7 节要求使用，不重新绘制。
其他区域的待核实复用要求：开始绘制前，检查目标文件中是否存在适合本页的状态栏、微信胶囊、按钮等内容。“顶部控件”“公共组件”“组件库”仅为搜索线索，不代表这些内容一定存在。
找到样式和状态匹配的组件时，插入对应实例，仅覆盖本页标题、文本及选中状态；已有实例可保留关联复制。普通 Frame/Group 应复制其可编辑图层，不要误称组件。保留共享主组件和源图层，禁止移动、解绑或修改共享原件。除必须复用的底部导航外，没有合适来源的部分再按截图绘制。

2. 整体布局与风格

采用截图中的浅蓝背景、白色圆角卡片和蓝色主按钮，保持清爽的校园小程序风格。
页面从上至下排列：系统状态栏、页面导航、图片上传卡片、四行基础信息卡片、描述卡片、发布操作栏、底部五栏导航、系统安全区。
导航下方直接进入图片卡片，不添加宣传标题、副标题、插画或横幅；特别不要恢复「让校园资源流动起来」及其下方文案。

参考色值：
- 页面背景约 #F0F6FE；
- 卡片为 #FFFFFF；
- 上传区域浅底约 #F2F8FE；
- 主按钮蓝约 #3481FB，主色规范可用 #3B82F6；
- 上传图标、上传文字、价格符号采用截图中略明亮的蓝色，约 #2677FF；底部导航的选中态沿用现有组件样式；
- 深色文字约 #111A36；
- 次要文字、占位文字约 #818A9E；未选中导航沿用现有组件样式；
- 分隔线约 #EDF1F7，上传虚线约 #9BC4FF。
以上均为近似参考，需结合截图校准。使用干净的纯色或轻微渐变表现截图，不把截图中的噪点、压缩纹理做成背景图片。阴影极轻，避免明显悬浮感。

字体优先使用 PingFang SC，缺失时选择相近的中文无衬线字体。页面标题约 17px 半粗；区块标题和字段标签约 15–16px 半粗；字段内容约 14–15px；辅助文字约 12px。底部导航字体、字号和字重沿用现有组件。

3. 顶部区域

顶部总高度约 82px，背景与页面浅蓝底衔接。
左上角显示「9:41」，右上角保留黑色信号、Wi-Fi 和电量图标。
下方页面标题「发布资源」水平居中。右侧为白色微信胶囊：圆角外形、轻灰边框，内部左侧三个黑点、中央细分隔线、右侧黑色圆形关闭图标。顶部不增加返回箭头。
在没有合适复用组件的情况下，使用矢量形状绘制这些控件，不使用截图切片。

4. 图片上传卡片

卡片约位于 x=12、y=83，宽 366px、高 169px；白色背景，圆角约 12–14px，左右内边距约 14px。
顶部左侧为「图片」，右侧为「0/9」，两者保持同一视觉基线。
内部上传区域约位于 x=26、y=125，宽 338px、高 112px，浅蓝填充、1px 蓝色虚线边框，圆角约 13px；虚线节奏细而均匀。
上传区域内部垂直排列并整体居中：
- 约 30px 的蓝色线框相机加号图标，圆角描边、透明内部；
- 蓝色文字「添加图片」；
- 灰色辅助文字「最多上传 9 张，实拍图更清晰。」。
相机、加号、两行文字均可独立编辑。当前为未上传图片状态，不出现缩略图、删除按钮或九个空格子。

5. 基础信息卡片

卡片约位于 x=12、y=263，宽 366px、高 228px；白色背景、圆角约 12–14px。
内部四行，行高约 56–58px。标签左边界约 x=26，内容左边界约 x=92。标签和内容在各行内垂直居中，行间使用细浅灰分隔线，分隔线不贴卡片边缘。

按以下顺序、类型与文字还原：
- 标题：单行文本输入框，占位文字「请输入标题，简明扼要」。
- 分类：选择控件，当前值「数码电子」为深色；右侧保留灰色向下箭头，菜单处于收起状态。
- 价格：金额输入框，蓝色「¥」与灰色占位文字「请输入价格」分为独立文本层。
- 面交：单行文本输入框，占位文字「如：荣昌校区 · E栋门口」。

输入区域不额外增加独立矩形边框。保留统一的行式表单布局，不增加字段图标、必填星号、价格选项或定位按钮。

6. 描述卡片

约位于 x=12、y=501，宽 366px、高 186px；白色背景，圆角与上方卡片一致。
左上角为深色半粗标题「描述」，内边距约 14px。
下方显示灰色占位文字「说说成色、用法、转让原因～」，其余区域保持截图中的输入留白。
这是独立可编辑的多行输入区域，不增加字符计数、模板标签、AI 按钮或额外说明。

7. 发布操作区与底部导航

发布操作区从 y≈699 开始，白色背景，宽度铺满画布。
主按钮约位于 x=14、y=705，宽 362px、高 44px，胶囊圆角约 22px；使用明亮蓝色及非常轻微的渐变，中央为白色半粗文字「发布」，字号约 17px。按截图呈现可用的蓝色外观，不因为当前字段为空自行改成禁用灰色。

底部导航必须复用当前页面已有的组件：先定位其现有实例与源组件，插入对应实例，或复制现有实例并保留组件关联。不要移动原页面中的实例，不重新绘制导航背景、图标、文字或分隔线，不解绑实例，不修改共享主组件。
保留现有组件的图标、字体、配色、尺寸、间距和布局。通过已有变体或组件属性将「发布」设为选中项，其余「首页、分类、消息、我的」为未选中态，五项顺序为「首页 / 分类 / 发布 / 消息 / 我的」。不得编造变体名称；若没有所需状态，只使用组件支持的局部覆盖。若仍无法表达或找不到用户指定组件，说明具体缺口，不擅自重绘替代。
将实例固定在页面底部，与画布等宽，按组件已有约束适配，避免拉伸图标和文字。截图 y≈760 仅作布局参考，最终按现有组件实际高度安排发布按钮与内容区，避免遮挡。
如果组件已经包含底部安全区或 Home 指示条，直接保留，不重复添加；若组件不包含，再按截图补充独立安全区和黑色 Home 指示条（约宽 138px、高 5px）。

8. 可编辑性与原型状态

所有中文、数字、货币符号保持独立文本层，不转轮廓。图标使用可编辑矢量；卡片、分隔线、虚线框、输入内容和按钮分别建层，不合并为大图。
新建的内容区域与表单行合理使用 Auto Layout 与约束，底部导航保留现有组件的布局结构。顶部导航和底部操作/导航区域在需要滚动预览时固定，内容不能被遮挡。
将新建的表单行、上传入口和主按钮整理为可复用的本地控件，或使用前述已核实实例；底部 Tab 项继续使用已有导航组件内部的实例与属性，不另建一套。新建图层命名清晰，例如「图片上传」「基础信息/标题」「基础信息/分类」「描述」「发布按钮」，现有组件内部图层保持原结构。
截图只展示默认态：上传 0/9、分类为数码电子、其他字段为空、发布 Tab 选中。先完整还原此状态。控件保留独立点击区域；没有已确认的目标页面或流程时，不自行添加弹窗、上传结果、发布成功页面或业务规则。

9. 完成后的核对与交付

保留一个干净的可编辑页面 Frame；参考截图如需留在文件中，应放在成品 Frame 外并锁定，不作为成品底图。
按相同比例对照截图，检查各区块位置、卡片尺寸、行高、字重、颜色、间距、所有文案及底部选中状态；可临时使用半透明截图叠加比对，交付时隐藏或移除叠加参考。
确认两行引导文案未被恢复、五个导航完整、所有文字可编辑、主体 UI 未被栅格化。必须确认底部导航来源于当前页面已有组件、实例关联保留、「发布」状态正确、共享主组件及原页面实例没有改变。
完成后返回该 Frame 的链接，并说明底部导航实际复用的组件名、来源节点及所用状态，同时简短说明其他复用内容和新建可编辑图层。
