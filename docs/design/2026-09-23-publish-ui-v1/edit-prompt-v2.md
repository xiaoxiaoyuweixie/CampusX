# 发布页 v2：移除引导文案

编辑来源：publish-ui.png。使用内置 image_gen，删除顶部两行引导文案，重新排列其余内容，底部操作区域固定。

```text
Edit the supplied mobile UI screenshot with one requested change: completely remove the two-line introduction between the top navigation and the 图片 upload card.
Delete both exact strings:
1. 让校园资源流动起来
2. 好物、资料与技能，都值得被发现。
Remove the whole introduction block including its reserved vertical spacing. Do not replace it with any other heading, subtitle, illustration, icon, or banner.

Reflow the existing upload card and information card upward so the 图片 card begins directly beneath the 发布资源 navigation, separated by a comfortable approximately 16 logical pixels. Preserve consistent 12px gaps between cards. Keep the bottom 发布 button, five-tab navigation and home indicator anchored at the bottom. Allow the existing description card to extend naturally into the reclaimed space so the layout has no conspicuous empty gap above the fixed action area.

Keep everything else visually faithful to the source: same blue-white palette, rounded cards, camera-plus upload icon, dashed upload area, typography, labels, field values/placeholders, row order, separator lines, button styling, selected 发布 tab and all four inactive tabs. Preserve the centered navigation title 发布资源, top status bar and WeChat capsule. Keep 图片, 0/9, 添加图片, 最多上传 9 张，实拍图更清晰。, 标题, 请输入标题，简明扼要, 分类, 数码电子, 价格, ¥ 请输入价格, 面交, 如：荣昌校区 · E栋门口, 描述, 说说成色、用法、转让原因～, and 发布.
One complete high-fidelity portrait screen with the same aspect ratio as input. No crop, added controls, external annotations, phone frame or comparison layout.
```
